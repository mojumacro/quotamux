---
name: Provider support
about: Ask for (or contribute) support for another subscription provider
labels: provider
---

**Provider**

**Usage endpoint** — URL, auth style, and where you found it (vendor CLI source,
`strings` on a binary, dashboard network tab). If you have not found one yet, say so;
that search is usually the hard part and we can help.

**Sample response** — redact any identifiers, keep the shape:

```json
```

**Quota shape** — does the vendor report absolute `remaining`/`limit`, remaining
percent, or used percent (utilization)? Per model or per subscription?
